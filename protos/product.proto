syntax = "proto3";

message Product {
    // The QR code assosciated identifier of the product
    string identifier = 1;

    // Name of the product
    string product_name = 2;

    // Assosciated shipment ids
    repeated string shipments = 3;
}
